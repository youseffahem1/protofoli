/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow serving the portal video from the public directory
  // Place portal.mp4 in your /public folder before deploying
};

module.exports = nextConfig;
